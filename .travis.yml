notifications:
  email:
    - kwoolery@gmail.com

sudo: true
dist: trusty
language:
  python:
  - '3.6'
install:
- pip install -r requirements.txt

deploy:
  provider: script
  script: bash scripts/deploy.sh
  on:
    branch: master
